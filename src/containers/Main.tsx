import React, { Component } from "react"
import { observer } from "mobx-react"

@observer
class Main extends Component {
    render() {
        return (
            <div>test
            </div>
        )
    }
}

export default Main;
